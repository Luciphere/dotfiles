function uall-fix --wraps='sudo rm -rf /var/lib/pacman/sync/*; and sudo pacman-key --init; and sudo pacman-key --populate archlinux cachyos; and sudo pacman -Sy archlinux-keyring cachyos-keyring; and sudo pacman -Syu' --wraps='sudo rm -rf /var/lib/pacman/sync/*; and sudo pacman-key --init; and sudo pacman-key --populate archlinux cachyos; and sudo pacman -Sy archlinux-keyring cachyos-keyring; and sudo cachyos-rate-mirrors; and sudo pacman -Syu' --description 'alias uall-fix=sudo rm -rf /var/lib/pacman/sync/*; and sudo pacman-key --init; and sudo pacman-key --populate archlinux cachyos; and sudo pacman -Sy archlinux-keyring cachyos-keyring; and sudo cachyos-rate-mirrors; and sudo pacman -Syu'
    sudo rm -rf /var/lib/pacman/sync/*; and sudo pacman-key --init; and sudo pacman-key --populate archlinux cachyos; and sudo pacman -Sy archlinux-keyring cachyos-keyring; and sudo cachyos-rate-mirrors; and sudo pacman -Syu $argv
end
